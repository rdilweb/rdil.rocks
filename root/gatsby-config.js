const plugins = [
    "gatsby-plugin-react-helmet",
    "gatsby-plugin-sitemap",
    "gatsby-plugin-svgr"
]

const siteMetadata = {
    siteUrl: "https://rdil.rocks/"
}

module.exports = {
    plugins,
    siteMetadata
}
